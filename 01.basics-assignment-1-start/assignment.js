const userInput = "15";
let result = "";

result = userInput + 18;

result = result - 2;
result = result * 20;
result = result / 3;

// the user input did not change becuase the user input is a constant and the value is refered to by the result variable.

alert("results = " + result);
alert("user input = " + userInput);
